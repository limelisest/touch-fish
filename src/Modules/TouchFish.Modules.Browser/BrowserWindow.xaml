<Window x:Class="TouchFish.Modules.Browser.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="网页" Width="760" Height="560"
        MinWidth="420" MinHeight="260"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        PreviewMouseDown="Window_OnPreviewMouseDown">
    <Border x:Name="WindowSurface"
            Background="{DynamicResource AppBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Padding="6"
                    Background="{DynamicResource SurfaceAlternateBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BackButton"
                            Grid.Column="0"
                            Width="32"
                            Height="32"
                            MinWidth="32"
                            MinHeight="32"
                            Margin="0,0,6,0"
                            Padding="0"
                            IsEnabled="False"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            ToolTip="返回"
                            Click="Back_OnClick">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="14"
                                   Text="&#xE72B;"
                                   TextAlignment="Center" />
                    </Button>

                    <Button x:Name="ForwardButton"
                            Grid.Column="1"
                            Width="32"
                            Height="32"
                            MinWidth="32"
                            MinHeight="32"
                            Margin="0,0,6,0"
                            Padding="0"
                            IsEnabled="False"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            ToolTip="前进"
                            Click="Forward_OnClick">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="14"
                                   Text="&#xE72A;"
                                   TextAlignment="Center" />
                    </Button>

                    <Button Grid.Column="2"
                            Width="32"
                            Height="32"
                            MinWidth="32"
                            MinHeight="32"
                            Margin="0,0,6,0"
                            Padding="0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            ToolTip="主页"
                            Click="Home_OnClick">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="14"
                                   Text="&#xE80F;"
                                   TextAlignment="Center" />
                    </Button>

                    <TextBox x:Name="AddressBox"
                             Grid.Column="3"
                             Height="32"
                             MinHeight="32"
                             Margin="0,0,6,0"
                             Padding="10,3"
                             Cursor="Arrow"
                             IsReadOnly="True"
                             VerticalContentAlignment="Center"
                             ToolTip="按住拖动窗口；单击后编辑地址"
                             PreviewMouseLeftButtonDown="AddressBox_OnPreviewMouseLeftButtonDown"
                             PreviewMouseMove="AddressBox_OnPreviewMouseMove"
                             PreviewMouseLeftButtonUp="AddressBox_OnPreviewMouseLeftButtonUp"
                             KeyDown="AddressBox_OnKeyDown"
                             LostKeyboardFocus="AddressBox_OnLostKeyboardFocus" />

                    <ComboBox x:Name="OpacitySelector"
                              Grid.Column="4"
                              Width="68"
                              Height="32"
                              MinHeight="32"
                              Margin="0,0,6,0"
                              FontSize="11"
                              ToolTip="窗口透明度"
                              SelectionChanged="OpacitySelector_OnSelectionChanged">
                        <ComboBoxItem Content="100%" Tag="1" FontSize="11" />
                        <ComboBoxItem Content="75%" Tag="0.75" FontSize="11" />
                        <ComboBoxItem Content="50%" Tag="0.5" FontSize="11" />
                        <ComboBoxItem Content="25%" Tag="0.25" FontSize="11" />
                    </ComboBox>

                    <Button Grid.Column="5"
                            Width="32"
                            Height="32"
                            MinWidth="32"
                            MinHeight="32"
                            Margin="0,0,6,0"
                            Padding="0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            ToolTip="刷新"
                            Click="Reload_OnClick">
                        <TextBlock FontFamily="Segoe UI Symbol"
                                   FontSize="17"
                                   Text="↻"
                                   TextAlignment="Center" />
                    </Button>

                    <Button Grid.Column="6"
                            Width="32"
                            Height="32"
                            MinWidth="32"
                            MinHeight="32"
                            Padding="0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            ToolTip="隐藏"
                            Click="Hide_OnClick">
                        <Grid Width="14" Height="14">
                            <Border Height="1.5"
                                    Margin="0,0,0,3"
                                    VerticalAlignment="Bottom"
                                    Background="{DynamicResource TextPrimaryBrush}"
                                    CornerRadius="1" />
                        </Grid>
                    </Button>
                </Grid>
            </Border>

            <wv2:WebView2CompositionControl x:Name="Browser"
                                            Grid.Row="1"
                                            DefaultBackgroundColor="White" />
        </Grid>
    </Border>
</Window>
