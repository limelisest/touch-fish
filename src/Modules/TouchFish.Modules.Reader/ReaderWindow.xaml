<Window x:Class="TouchFish.Modules.Reader.ReaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TouchFish 看书"
        Width="560" Height="420" MinWidth="320" MinHeight="220"
        WindowStyle="ToolWindow" ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False" Topmost="True"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button MinWidth="70" Padding="10,5" Content="上一章" Click="PreviousChapter_OnClick" />
            <TextBlock x:Name="ChapterTitle" Grid.Column="1" Margin="12,0"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
            <Button Grid.Column="2" MinWidth="70" Padding="10,5" Content="下一章" Click="NextChapter_OnClick" />
        </Grid>
        <TextBox x:Name="ReaderText" Grid.Row="2"
                 Padding="14" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 FontSize="16" TextBlock.LineHeight="26" />
    </Grid>
</Window>
